{
  "name": "devlens-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Unit tests for Devlens",
  "scripts": {
    "test": "npx vitest run --reporter=verbose",
    "test:watch": "npx vitest --reporter=verbose"
  },
  "devDependencies": {
    "vitest": "^4.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
